package com.interview.prep.amazon;

public class PostOrderTraversalSequences_BinarySearchTrees {

	/*
	 * Determine whether an input array is a post-order traversal sequence of a binary tree or not. 
	 * If it is, return true; otherwise return false. Assume all numbers in an input array are unique
	 */
	
	/**
	 * For example, if the input array is {5, 7, 6, 9, 11, 10, 8}, true should be returned, 
	 * since it is a post-order traversal sequence of the binary search tree in Figure 1. 
	 * If the input array is {7, 4, 6, 5}, 
	 * false should be returned since there are no binary search trees whose post-order traversal sequence is such an array.
	 * @param args
	 */
	public static void main(String[] args) {
		
	}
}
